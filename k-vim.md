# k-vim 快捷键速查表 (Skynet & Gin 增强版)

这份文档整理了 `k-vim` 针对 **Skynet (C/Lua)** 和 **Gin (Go)** 开发优化的常用快捷键。

## 1. 核心：IDE 增强 (coc.nvim + LSP)
依赖于 `clangd` (C/C++), `gopls` (Go), `lua-language-server` (Lua)。

| 快捷键 | 功能说明 | 备注 |
| `,jd` | **转到定义** (GoTo Definition) | 跳转到变量、函数定义处 |
| `K` | **查看文档** (Hover) | 在浮窗显示函数签名、文档说明 |
| `,rn` | **全局重命名** (Rename) | 批量修改变量名/函数名，支持跨文件 |
| `,gr` | **查找引用** (References) | 查找哪里用到了这个变量/函数 |
| `,o` | **代码大纲** (Outline) | 在侧边栏显示当前文件的函数列表 |
| `[g` / `]g` | **跳转错误点** | 在代码报错 (Error/Warning) 间快速跳转 |
| `,ac` | **快速修复** (Code Action) | 弹出修复建议（如自动 import） |
| `Ctrl+f / b` | **滚动浮窗** | 当 `K` 弹出的文档太长时，控制浮窗滚动 |
| `,se` | **错误列表** (Show Errors) | 开启/关闭语法错误列表窗口 |

## 2. 编程语言专有特性
| 特性 | 场景 | 功能说明 |
| :--- | :--- | :--- |
| **自动格式化** | Go (Gin) | 按 `,w` 保存时自动运行 `goimports` |
| **4空格缩进** | Lua/C (Skynet) | 符合 Skynet 规范 |
| **Tab 缩进** | Go | 遵循 Go 官方标准 |
| `F10` | 通用 | **QuickRun**：直接运行当前 C/Go/Lua 脚本 |

## 3. 文件、函数与全局搜索
| 快捷键 | 功能说明 | 补充 |
| :--- | :--- | :--- |
| `,n` | **NERDTree**：打开左侧文件浏览器 | |
| `,p` | **CtrlP**：模糊搜索并打开项目中的文件 | |
| `,sv` | **左右分屏并搜索** | **终极推荐**：Normal 模式按，无任何冲突。 |
| `,sx` | **上下分屏并搜索** | **终极推荐**：Normal 模式按，无任何冲突。 |
| `,p` 搜索框内 | **分屏打开选中文件** | **`F2`** 左右分屏；**`F3`** 上下分屏。 |
| `\` | **CtrlSF**：全局全文搜索（搜索框架内所有调用） | |
| `Space` | 进入搜索模式 (等同于 `/`) | |
| `,/` | 取消当前的搜索高亮 | |

## 4. 高效代码编辑
| 快捷键 | 功能说明 |
| :--- | :--- |
| `kj` | **替代 `<Esc>`** (手不用离开主键盘区) |
| `;` | **替代 `:`** (进入命令行模式) |
| `,ss` | **加载配置** (Settings Source) | 重新加载 .vimrc 配置 |

| `,a` | **代码对齐** (选中后 `,a=` 按等号对齐，`,a:` 按冒号) |
| `v / V` | **智能扩大/缩小选中** (逐层选中括号内容) |
| `Ctrl+m` | **多光标编辑** (选中多个相同单词同时修改) |
| `> / <` | 调整缩进 (操作后会自动重新选中) |

## 5. 窗口与标签 (Tab) 管理
| 快捷键 | 功能说明 |
| :--- | :--- |
| `Ctrl + h/j/k/l` | 在**左/下/上/右**分屏窗口间跳转 |
| `,z` | **窗口最大化/还原**切换 |
| `,w` / `,q` | 快速**保存** / 快速**退出** |
| `Ctrl + t` | 新建标签页 (Tab) |
| `,1` - `,9` | 快速跳转到第 1-9 个标签页 |
| `,tt` | 在最近打开的两个标签页间快速来回切换 |

## 6. 版本控制 (Git) 与 历史
| 快捷键 | 功能说明 |
| :--- | :--- |
| **侧边栏标记** | 行号左侧实时显示 `+` (增) `-` (删) `~` (改) |
| `,gs` | 手动开关 GitGutter 修改标记显示 |
| `,ge` | **Gdiff**：查看当前文件与 Git 库的差异 |
| `,h` | **Gundo**：打开撤销历史树，找回之前的代码 |

---
*Generated by Gemini CLI for k-vim customized environment.*
